// GameOverScene.swift
// Game over screen: "GAME OVER" in magenta, score tally, explosion at bottom.
// Transitions to initials entry if high score, otherwise to TitleScene.
// Phase 1+: full implementation.

import SpriteKit

final class GameOverScene: SKScene {
    var finalScore: Int = 0
    var levelReached: Int = 1

    // Phase 1+: score tally animation, explosion fireball, initials entry transition
}
