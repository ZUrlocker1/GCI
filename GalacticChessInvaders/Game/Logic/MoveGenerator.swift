// MoveGenerator.swift
// Wraps ChessKit to expose legal moves for both the AI strategist
// and the player's piece selection UI.
// Phase 1+: full implementation after ChessKit SPM package is added.

import Foundation
// import ChessKit  ← uncomment once added via SPM in Xcode

final class MoveGenerator {
    // Phase 1: ChessKit-backed legal move generation
    // Phase 2: aggressive-mode weighting for Level 2+ engine
}
