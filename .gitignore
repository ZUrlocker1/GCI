# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM
build/

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store
**/.DS_Store
._*
.Spotlight-V100
.Trashes

# Xcode user-specific files
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/

# Instruments
*.trace

# VS Code
.vscode/
*.code-workspace

# Claude Code
.claude/settings.local.json

# Archives
*.zip
*.tar.gz

# Test results
TestResults/

# Audio source files — large binaries, regenerate with assets/sfx/convert_to_caf.sh
assets/sfx/

# Generated icon variants — the chosen one is installed as Resources/AppIcon.icns
icon_options/

# Reference listening, not ours to redistribute.
Tempest 2000 OST/

# Reference material, kept locally and deliberately out of the repo. These are
# ~360MB of Abstraction / Tallbeard loop packs that nothing in the game uses;
# they were tracked once and pushed, which is why the history is large.
assets/music/music-loop-bundle-*/
